# 実装メモ: 小節感BGM・セリフ見切れ修正・生物図鑑化

## 1. 自動生成BGMの小節感追加

`GeneratedBGMManager` を更新し、単発の重みづけランダムから、拍・小節・4小節句を持つ生成方式へ変更した。

### 追加した考え方

- `beatsPerMeasure`
  - 通常マップ・戦闘・無音の核は4拍子基準
  - イベントは3拍子または4拍子を起動時に選択
- `beatIndex`
  - 小節内の拍位置
- `measureIndex`
  - 小節番号
- `measureIndex % 4`
  - 4小節単位の句の位置

### 生成仕様

- 1拍目は低音が鳴りやすい
- 4小節単位の先頭と終端は根音・五度へ戻りやすい
- 弱拍は休符が多い
- 音は最大3音まで同時に鳴る
- 低音 + 任意の上声1〜2音で和音を作る
- 前回和音の緊張度が高い場合、次は根音・五度に戻りやすい
- 戦闘中は従来よりさらに低音量・低密度
- 無音の核はほぼ無音を維持

## 2. セリフ見切れ修正

`StoryEventPagerView` を `GeometryReader` ベースに変更した。

### 修正内容

- セリフ枠の幅を `min(660, 画面幅 - 36)` に制限
- セリフ枠を画面内に必ず収める
- 左右パディングを固定化
- 背景画像は画面サイズに合わせて `clipped()`
- セリフ本文は `frame(maxWidth: .infinity, alignment: .leading)` で折り返し

## 3. 生物図鑑表示

資料画面の「生物録」を「生物図鑑」に変更し、図鑑カード形式へ整理した。

### 修正内容

- タブ名: `生物録` → `生物図鑑`
- `CreatureArchiveSection` をカードグリッド化
- No.、生物アイコン、ラベル、答えの音、距離、五度環、説明文を表示
- スマートフォン幅では2列程度で並ぶよう `LazyVGrid(.adaptive)` を使用

## 確認

- `swiftc -parse` による構文チェック通過
- `xcodebuild` による実機ビルド確認は未実施
