# 響き一覧モード：固定13列表示

## 変更内容

手帳の響き一覧モードについて、横スクロールを廃止し、インターバル生物の種類ごとに固定列を割り当てる方式に変更した。

## 表示仕様

- 一行一項目の方針は維持。
- 左側に代表コード名／スケール名を1つだけ表示。
- 右側は固定13列で表示。
- 列は五度環上の位置順で `-6 ... 0 ... +6` に固定。
- 該当するインターバル生物がある列にだけ小さいアイコンを表示。
- 該当しない列は薄い空セルとして表示。
- 0列は根を表示。
- トライトーンは既存の分類ロジックに従い、必要に応じて -6 側または +6 側に表示する。分類が中立のときは両側に表示される。
- 一覧モード中は、並び順は固定で種類順扱いとし、並び順セグメントは無効表示にした。

## 確認

- `swiftc -parse IntervalBattleMVP/*.swift` 通過。
- 既存の簡易／詳細モードの表示ロジックは維持。
