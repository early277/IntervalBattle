# 実装メモ: プロローグ順序・父の助言・残響バー・背景音廃止

## 変更内容

- プロローグの父の手帳を渡す場面を最後へ移動。
- タイトル画面に `field_map_bg` を背景として表示。
- 戦闘画面のMP下に表示していた残響ゲージを削除。
- 父の助言を敵画像の近くに表示するよう変更。
  - インターバル生物との関係を見ながら読めるよう、敵画像に重なる位置で表示。
  - 2秒後に表示され、次の回答まで残る既存仕様は維持。
- 背景音機能をUIから削除し、画面遷移時も再生せず停止するよう変更。
- 父の手帳 > 響き の並び名を `現行順` から `種類順` に変更。
- 響きの `高さ順` ではトライトーンを二重表示しないよう変更。
  - `種類順` では従来通り、文脈に応じて -6 / +6 を表示。

## 確認

- `swiftc -parse` による構文チェック通過。
- Xcode実機ビルド・実機表示確認は未実施。
