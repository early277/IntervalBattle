# 父の手帳・背景音モチーフ・コード絞り込み 実装メモ

## 背景音
- GeneratedBGMManager に 2拍 / 1小節 / 2小節 / 4小節 / 8小節のモチーフ単位を追加。
- リズム、低音、高音側の音が完全ランダムではなく、一定周期で再帰しやすいように重み付け。
- 8小節相当の境目でモチーフを再生成し、同じ形が続きすぎないようにした。

## 父の手帳
- 「音環書庫」表記を廃止し、画面全体を「父の手帳」とした。
- マップ上のボタンは「手帳」に変更。
- 手帳内タブは「使い方」「生物録」「コード」に整理。
- プロローグに、父が古い書物を読み解いて手帳にまとめ、主人公へ渡す流れを追加。
- 初回マップ表示時に、手帳と強化画面の開き方を案内するシステム表示を追加。

## コード・スケール表示
- コード内の音の並びを「現行順」と「高さ順」で切替可能にした。
- 調指定フィルタを追加。
- 選んだ調の外にある音を何音まで許容するか、0〜7音で指定できるようにした。

## マップ初期表示
- 初めてマップを表示する際、最初の街 C の灯火港が中央付近に見えるよう、初期スクロール位置を横方向中央へ寄せる処理を追加。

## 確認
- swiftc -parse による構文チェックは通過。
- xcodebuild / 実機での動作確認は未実施。
