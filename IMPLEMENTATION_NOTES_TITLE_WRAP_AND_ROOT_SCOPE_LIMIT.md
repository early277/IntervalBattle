# タイトル見切れ・すべての根表示制限 修正

## 修正日
2026-06-29

## 変更内容

### 1. タイトル画面の文字見切れ修正

`TitleView` を `GeometryReader` ベースに変更し、タイトル、サブタイトル、導入文カード、ボタンの幅を画面幅内に明示的に制限した。

- 背景画像は画面サイズに固定
- タイトル・サブタイトルは `contentWidth` 内に収める
- 導入文カードは `contentWidth` 内で折り返す
- 導入文の `fixedSize` を外し、折り返し優先に変更

これにより、背景画像追加後に導入文の先頭が画面外へ見切れる問題を修正。

### 2. 「すべての根」の表示条件を変更

父の手帳 > 響き で、`すべての根` を常時押せると表示件数が大きくなり操作が重くなるため、調指定中のみ表示するようにした。

- 調指定なし：`すべての根` ボタンを非表示
- 調指定あり：`この根だけ / すべての根` 切替を表示
- 調指定を解除した時点で `searchAllRoots = false`
- 内部計算も `effectiveSearchAllRoots` を通し、調指定なしでは必ず現在の根だけを対象にする

## 影響範囲

- `ContentView.swift`
  - `TitleView`
  - `HarmonyArchiveSection`

## 確認

`swiftc -parse` による構文チェック通過。
