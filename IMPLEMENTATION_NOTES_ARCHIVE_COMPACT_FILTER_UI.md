# 書庫UI調整メモ

## 変更内容

- 書庫の分類検索を、左右一列の分類バーへ変更。
  - 左側は中央0から左へ動かすほど `-1` 〜 `-6`。
  - 右側は中央0から右へ動かすほど `+1` 〜 `+6`。
  - `全て` ボタンは分類バーとは別行に配置し、バー幅を広く確保。
- `一覧 / 詳細` 切替を追加。
  - `詳細` は従来に近い表示。
  - `一覧` は説明文と距離テキストを省き、コード表記・読み・分類ゲージ・インターバル生物を優先表示。
- 分類ゲージの端の `-6` / `+6` 表示を削除。
  - 1単位の目盛りは残す。
  - 分類端点の数字と中央0は表示。
- インターバル生物画像の表示サイズは維持しつつ、余白を削減。

## 意図

書庫を詳細に読む画面だけではなく、分類で絞り込みながら多くのコード・スケールを一覧できる画面として使えるようにする。
