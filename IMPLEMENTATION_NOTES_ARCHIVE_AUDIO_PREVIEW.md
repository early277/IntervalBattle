# 書庫・生物録 試聴機能 実装メモ

## 実装日
2026-06-29

## 内容

### 生物録
- 生物録の各項目をタップしたとき、現在選択中の根音と、そのインターバル生物に対応する音を同時に鳴らす。
- 根音は低い音、対応音は一オクターブ上の音として鳴らす。
- 表示形式・文言は変更しない。

### 書庫
- 書庫の各コード／スケール項目をタップしたときに試聴できるようにした。
- コードは、ルートを最低音に固定して和音として鳴らす。
- ♭9 / 9 などのテンション系は高い位置に配置し、響きの色が上側で分かるようにした。
- スケールは和音ではなく、根音から順に短く鳴らす。
- スケール判定は書庫内のスケール項目名で行う。

## 実装ファイル
- `GameModel.swift`
  - `TonePlayer.playArchiveInterval(...)`
  - `TonePlayer.playArchiveHarmony(...)`
  - コード用ボイシング生成
  - スケール用逐次再生
- `ContentView.swift`
  - `CreatureArchiveSection` の項目タップで試聴
  - `HarmonyArchiveSection` の項目タップで試聴

## 方針
- 既存の背景音とは別の短い確認音として扱う。
- 書庫・生物録の学習を補助するための音であり、BGMとは分離する。
