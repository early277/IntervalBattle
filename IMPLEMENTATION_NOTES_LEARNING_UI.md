# 学習導線・書庫・鍵盤UI 実装メモ

## 反映内容

- 指導集を廃止し、`音環手帳・音環書庫` へ整理。
  - `手帳`: 操作、技、宝玉、鍵盤の見方。
  - `生物録`: インターバル生物の画像、距離、選択オーラでの答え。
  - `書庫`: コード・スケールの構成音とインターバル生物画像。
- 書庫・手帳を最初から開けるようにマップに常設。
- 父の助言を、直接の答えではなく「思い出した言葉」に変更。
- 技名を戦闘中は機能名優先に変更。
  - 根標 → 目印
  - 緩拍 → 減速
  - 気配断ち → 隠れる
  - 癒響 → 回復
- 目印を拡張。
  - 根の鍵を鍵盤上に表示。
  - 使用1秒後、敵の近くに「右へN / 左へN」を表示。
  - 効果は時間ではなく鍵盤入力回数で消える。
  - 基本回数: Lv1=5回, Lv2=6回, Lv3=7回, Lv4=8回, Lv5=9回。
  - 延響律による技効果時間増加は、3秒=1入力回として追加回数に換算。
- 回復の再使用間隔を 45秒 → 12秒へ短縮。
- 鍵盤を1オクターブ+同音1鍵表示に変更。
  - 右端に左端と同じ音を再表示。
  - 黒鍵も端まで表示し、位置認識を補助。
- コード/スケール資料にインターバル生物画像を表示。

## 未実施・要確認

- 効果音は既存の SystemSound と TonePlayer を維持。専用音源ファイル化は未実施。
- 実機Xcodeビルドは未確認。`swiftc -parse` による Swift 構文確認のみ実施。


## 追加修正: 距離確認技・書庫拡張
- 目印から距離表示を分離し、新しい初期技「距離」を追加。
- 距離は低MP・インターバルなし・1秒後に一度だけ表示。拍戻しの対象外。
- プロローグの会話ボックス内からシーン名の大見出しを削除。
- 生物録の距離表示を「右へX / 左へY」に変更。
- 書庫のコード・スケール例を大幅追加し、各構成音に対応するインターバル生物画像を表示。
- 鍵盤端の黒鍵を半分見える形で描画し、端の接続感と黒鍵配列を補助。
