# 実装メモ：書庫のインターバル生物順序・生物録説明修正

## 変更内容

- 書庫のコード・スケール内のインターバル生物表示順を、五度円上の分類順へ変更。
  - マイナス側 → 根 → プラス側の順。
  - 例：マイナー7は 3−、7−、根、5 の順に表示。
- トライトーンを含む場合の表示位置を、分類ゲージと同じ判定に変更。
  - プラス優勢なら +6 側。
  - マイナス優勢なら -6 側。
  - 同数なら -6 と +6 の両方に表示。
- 生物録の末尾文を削除し、根と一緒に鳴らした時の簡潔な印象文へ変更。
  - 「まず姿を見て、必要なら『距離』で一度だけ確かめます。」は削除。

## 確認

- `swiftc -parse GameModel.swift ContentView.swift IntervalBattleMVPApp.swift` 通過。
