# 実装メモ: 父の助言・響き・プロローグ整合性修正

## 変更内容

### 父の手帳
- 手帳内タブ `コード` を `響き` に変更。
- コードとスケールは分けず、根のまわりに置かれる音の型として同じ一覧で扱う方針を維持。

### 技・助言
- ルート表示技 `目印` を通常の戦闘技から廃止。
  - 初期装備から削除。
  - `isActiveBattleSkill` から削除。
  - データバージョンを更新し、既存保存データでも初期装備から外れるようにした。
- `距離` を `父の助言` に変更。
  - 消費MP 0。
  - 回数制限なし。
  - 再使用インターバルなし。
  - 使用後2秒で父のセリフとして助言を表示。
  - 助言は次に鍵盤を押すまで残る。
- 初回の助言だけ、数え方を丁寧に説明。
  - 根の音は0。
  - となりを1つ目として数える。
  - 右/左の両方の数を示し、今回はどちらへ進むかを示す。
- 2回目以降は短い助言にする。

### プロローグ
- 旧友が父の調律の音を封じる描写を追加。
- 父が直接中心へ進めない理由を明確化。
- 父の主人公への呼び方は、原則として直接呼称を避ける文に調整。

### 表示文言
- `技` 説明を `父の助言と技` に変更。
- 強化画面では `父の助言` を `助言` として表示し、`消費なし` と表示。
