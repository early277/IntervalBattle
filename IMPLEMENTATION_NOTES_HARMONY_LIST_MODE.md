# 響き辞典：一行一覧表示モード追加

## 反映内容

- 父の手帳内「響き」表示に第三の表示モードを追加。
  - 簡易
  - 詳細
  - 一覧
- 従来の「一覧」相当の表示名は「簡易」に変更。
- 新しい「一覧」モードは、一行につき一つのコード／スケールのみを表示。
- 各行の左側には代表的なコードネーム／スケール名を1つだけ表示。
  - `notationTemplates` の先頭要素のみを採用。
  - 別表記は表示しない。
- 右側には該当する音の生き物だけを横一列で表示。
  - 読み、説明文、分類ゲージ、距離、音名は表示しない。
  - 横に長いスケールは横スクロールで確認。
- 行タップで従来どおり該当コード／スケールを鳴らす。
- 英語・韓国語の表示名も追加。
  - 簡易: Compact / 간단
  - 一覧: List / 목록

## 確認

- `swiftc -parse IntervalBattleMVP/*.swift` 通過。
- `plutil -lint IntervalBattleMVP/*.lproj/*.strings` 通過。
