# タイトル画面見切れ・すべての根検索 修正メモ

## 修正内容

- タイトル画面の背景画像 `field_map_bg` がレイアウトに影響しないよう、背景画像に `.frame(maxWidth: .infinity, maxHeight: .infinity)` と `.clipped()` を追加。
- タイトル・サブタイトルに `lineLimit`、`minimumScaleFactor`、横幅制約を追加し、文字見切れを抑制。
- 父の手帳 > 響き に `この根だけ / すべての根` の切替ボタンを追加。
- `すべての根` 選択時は、現在選択中の根だけでなく、C〜B のすべての根へ展開して検索・表示する。
- 根指定フィルタは、すべての根展開後の実音に対して判定する。
- すべての根表示時も、項目タップの試聴は表示されているルートで鳴る。

## 確認

- `swiftc -parse` による構文チェックを通過。
